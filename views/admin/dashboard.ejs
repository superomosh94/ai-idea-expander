<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard | AI Idea Expander</title>
    <link rel="icon" type="image/png" href="/favicon.png">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.2/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body class="d-flex flex-column min-vh-100 bg-light">
    <%- include('../partials/navbar') %>

        <main class="flex-grow-1 py-4">
            <div class="container-fluid">
                <%- include('../partials/flash-messages') %>

                    <!-- Page Header -->
                    <div class="row mb-4">
                        <div class="col-12">
                            <div class="d-flex justify-content-between align-items-center">
                                <div>
                                    <h2 class="fw-bold mb-1">
                                        <i class="bi bi-speedometer2 text-primary me-2"></i>Admin Dashboard
                                    </h2>
                                    <p class="text-muted mb-0">Manage users, ideas, and system settings</p>
                                </div>
                                <div>
                                    <span class="badge bg-success fs-6">
                                        <i class="bi bi-circle-fill me-1" style="font-size: 0.5rem;"></i>System Active
                                    </span>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Statistics Cards -->
                    <div class="row g-4 mb-4">
                        <div class="col-xl-3 col-md-6">
                            <div class="card border-0 shadow-sm h-100">
                                <div class="card-body">
                                    <div class="d-flex justify-content-between align-items-start">
                                        <div>
                                            <p class="text-muted mb-2 small">Total Users</p>
                                            <h3 class="fw-bold mb-0">
                                                <%= stats.totalUsers %>
                                            </h3>
                                        </div>
                                        <div class="bg-primary bg-opacity-10 p-3 rounded">
                                            <i class="bi bi-people-fill text-primary fs-4"></i>
                                        </div>
                                    </div>
                                    <div class="mt-3">
                                        <small class="text-success">
                                            <i class="bi bi-arrow-up"></i>
                                            <%= stats.newUsersThisMonth %> this month
                                        </small>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="col-xl-3 col-md-6">
                            <div class="card border-0 shadow-sm h-100">
                                <div class="card-body">
                                    <div class="d-flex justify-content-between align-items-start">
                                        <div>
                                            <p class="text-muted mb-2 small">Total Ideas</p>
                                            <h3 class="fw-bold mb-0">
                                                <%= stats.totalIdeas %>
                                            </h3>
                                        </div>
                                        <div class="bg-success bg-opacity-10 p-3 rounded">
                                            <i class="bi bi-lightbulb-fill text-success fs-4"></i>
                                        </div>
                                    </div>
                                    <div class="mt-3">
                                        <small class="text-success">
                                            <i class="bi bi-arrow-up"></i>
                                            <%= stats.ideasToday %> today
                                        </small>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="col-xl-3 col-md-6">
                            <div class="card border-0 shadow-sm h-100">
                                <div class="card-body">
                                    <div class="d-flex justify-content-between align-items-start">
                                        <div>
                                            <p class="text-muted mb-2 small">Active Users</p>
                                            <h3 class="fw-bold mb-0">
                                                <%= stats.activeUsers %>
                                            </h3>
                                        </div>
                                        <div class="bg-warning bg-opacity-10 p-3 rounded">
                                            <i class="bi bi-person-check-fill text-warning fs-4"></i>
                                        </div>
                                    </div>
                                    <div class="mt-3">
                                        <small class="text-info">
                                            <i class="bi bi-clock"></i> Last 7 days
                                        </small>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="col-xl-3 col-md-6">
                            <div class="card border-0 shadow-sm h-100">
                                <div class="card-body">
                                    <div class="d-flex justify-content-between align-items-start">
                                        <div>
                                            <p class="text-muted mb-2 small">API Usage</p>
                                            <h3 class="fw-bold mb-0">
                                                <%= stats.apiCalls %>
                                            </h3>
                                        </div>
                                        <div class="bg-info bg-opacity-10 p-3 rounded">
                                            <i class="bi bi-cpu-fill text-info fs-4"></i>
                                        </div>
                                    </div>
                                    <div class="mt-3">
                                        <small class="text-muted">
                                            <i class="bi bi-graph-up"></i> This month
                                        </small>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Recent Users & Recent Ideas -->
                    <div class="row g-4">
                        <!-- Recent Users -->
                        <div class="col-lg-6">
                            <div class="card border-0 shadow-sm">
                                <div class="card-header bg-white py-3">
                                    <div class="d-flex justify-content-between align-items-center">
                                        <h5 class="mb-0 fw-bold">
                                            <i class="bi bi-people text-primary me-2"></i>Recent Users
                                        </h5>
                                        <a href="/admin/users" class="btn btn-sm btn-outline-primary">View All</a>
                                    </div>
                                </div>
                                <div class="card-body p-0">
                                    <div class="table-responsive">
                                        <table class="table table-hover mb-0">
                                            <thead class="table-light">
                                                <tr>
                                                    <th>Name</th>
                                                    <th>Email</th>
                                                    <th>Role</th>
                                                    <th>Status</th>
                                                    <th>Joined</th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                <% if (recentUsers && recentUsers.length> 0) { %>
                                                    <% recentUsers.forEach(user=> { %>
                                                        <tr>
                                                            <td class="fw-semibold">
                                                                <%= user.name %>
                                                            </td>
                                                            <td class="text-muted small">
                                                                <%= user.email %>
                                                            </td>
                                                            <td>
                                                                <span
                                                                    class="badge <%= user.role === 'admin' ? 'bg-danger' : 'bg-secondary' %>">
                                                                    <%= user.role %>
                                                                </span>
                                                            </td>
                                                            <td>
                                                                <% if (user.is_active) { %>
                                                                    <span class="badge bg-success">Active</span>
                                                                    <% } else { %>
                                                                        <span class="badge bg-danger">Inactive</span>
                                                                        <% } %>
                                                            </td>
                                                            <td class="text-muted small">
                                                                <%= new Date(user.created_at).toLocaleDateString() %>
                                                            </td>
                                                        </tr>
                                                        <% }) %>
                                                            <% } else { %>
                                                                <tr>
                                                                    <td colspan="5" class="text-center text-muted py-4">
                                                                        No users found
                                                                    </td>
                                                                </tr>
                                                                <% } %>
                                            </tbody>
                                        </table>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Recent Ideas -->
                        <div class="col-lg-6">
                            <div class="card border-0 shadow-sm">
                                <div class="card-header bg-white py-3">
                                    <div class="d-flex justify-content-between align-items-center">
                                        <h5 class="mb-0 fw-bold">
                                            <i class="bi bi-lightbulb text-warning me-2"></i>Recent Ideas
                                        </h5>
                                        <a href="/admin/ideas" class="btn btn-sm btn-outline-warning">View All</a>
                                    </div>
                                </div>
                                <div class="card-body p-0">
                                    <div class="table-responsive">
                                        <table class="table table-hover mb-0">
                                            <thead class="table-light">
                                                <tr>
                                                    <th>Title</th>
                                                    <th>User</th>
                                                    <th>Status</th>
                                                    <th>Created</th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                <% if (recentIdeas && recentIdeas.length> 0) { %>
                                                    <% recentIdeas.forEach(idea=> { %>
                                                        <tr>
                                                            <td class="fw-semibold">
                                                                <%= idea.title.substring(0, 40) %>...
                                                            </td>
                                                            <td class="text-muted small">
                                                                <%= idea.user_name %>
                                                            </td>
                                                            <td>
                                                                <% let badgeClass='' ; switch(idea.status) {
                                                                    case 'completed' : badgeClass='bg-success' ; break;
                                                                    case 'processing' : badgeClass='bg-info' ; break;
                                                                    case 'failed' : badgeClass='bg-danger' ; break;
                                                                    default: badgeClass='bg-secondary' ; } %>
                                                                    <span class="badge <%= badgeClass %>">
                                                                        <%= idea.status %>
                                                                    </span>
                                                            </td>
                                                            <td class="text-muted small">
                                                                <%= new Date(idea.created_at).toLocaleDateString() %>
                                                            </td>
                                                        </tr>
                                                        <% }) %>
                                                            <% } else { %>
                                                                <tr>
                                                                    <td colspan="4" class="text-center text-muted py-4">
                                                                        No ideas found
                                                                    </td>
                                                                </tr>
                                                                <% } %>
                                            </tbody>
                                        </table>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
            </div>
        </main>

        <%- include('../partials/footer') %>

            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
            <script src="/js/main.js"></script>
</body>

</html>