<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %> | AI Idea Expander
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.2/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body class="d-flex flex-column min-vh-100 bg-light">
    <%- include('../partials/navbar') %>

        <main class="flex-grow-1 py-5">
            <div class="container">
                <div class="row justify-content-center">
                    <div class="col-lg-10">
                        <%- include('../partials/flash-messages') %>

                            <div class="text-center mb-5">
                                <h1 class="fw-bold mb-3">AI Idea Generator</h1>
                                <p class="lead text-muted">Describe a field, context, or hackathon theme, and let AI
                                    generate innovative ideas for you.</p>
                            </div>

                            <!-- Input Form -->
                            <div class="card border-0 shadow-sm mb-5">
                                <div class="card-body p-4">
                                    <form method="POST" action="/ideas/generate" class="row g-3 align-items-end">
                                        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                        <div class="col-md-9">
                                            <label for="context" class="form-label fw-semibold">Discussion Topic / Field
                                                / Context</label>
                                            <input type="text" class="form-control form-control-lg" id="context"
                                                name="context"
                                                placeholder="e.g. AI for Healthcare, Blockchain Hackathon, Educational Game..."
                                                value="<%= context %>" required>
                                        </div>
                                        <div class="col-md-3">
                                            <button type="submit" class="btn btn-primary btn-lg w-100">
                                                <i class="bi bi-magic me-2"></i>Generate
                                            </button>
                                        </div>
                                    </form>
                                </div>
                            </div>

                            <!-- Results Section -->
                            <% if (ideas && ideas.length> 0) { %>
                                <h4 class="fw-bold mb-4">Generated Ideas for "<%= context %>"</h4>
                                <div class="row g-4">
                                    <% ideas.forEach((idea, index)=> { %>
                                        <div class="col-md-6">
                                            <div class="card h-100 border-0 shadow-sm hover-lift">
                                                <div class="card-body p-4 d-flex flex-column">
                                                    <div class="mb-3">
                                                        <span
                                                            class="badge bg-primary bg-opacity-10 text-primary mb-2">Option
                                                            <%= index + 1 %></span>
                                                        <h4 class="card-title fw-bold">
                                                            <%= idea.title %>
                                                        </h4>
                                                    </div>
                                                    <p class="card-text text-muted flex-grow-1">
                                                        <%= idea.description %>
                                                    </p>

                                                    <div class="mt-4 pt-3 border-top">
                                                        <form method="GET" action="/ideas/create">
                                                            <input type="hidden" name="title" value="<%= idea.title %>">
                                                            <input type="hidden" name="raw_idea"
                                                                value="<%= idea.description %>">
                                                            <button type="submit" class="btn btn-outline-primary w-100">
                                                                <i class="bi bi-check-circle-fill me-2"></i>Select &
                                                                Refine
                                                            </button>
                                                        </form>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                        <% }); %>
                                </div>
                                <% } %>
                    </div>
                </div>
            </div>
        </main>

        <%- include('../partials/footer') %>

            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
            <script src="/js/main.js"></script>
</body>

</html>