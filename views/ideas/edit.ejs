<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %> | AI Idea Expander
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.2/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body class="d-flex flex-column min-vh-100 bg-light">
    <%- include('../partials/navbar') %>

        <main class="flex-grow-1 py-5">
            <div class="container">
                <div class="row justify-content-center">
                    <div class="col-lg-8">
                        <%- include('../partials/flash-messages') %>

                            <div class="card border-0 shadow-lg">
                                <div class="card-header bg-primary text-white py-4">
                                    <h2 class="mb-0 fw-bold">
                                        <i class="bi bi-pencil-square me-2"></i>Edit Idea
                                    </h2>
                                </div>
                                <div class="card-body p-5">
                                    <form method="POST" action="/ideas/<%= idea.id %>/edit">
                                        <input type="hidden" name="_csrf" value="<%= csrfToken %>">

                                        <div class="mb-4">
                                            <label for="title" class="form-label fw-semibold">
                                                Idea Title <span class="text-danger">*</span>
                                            </label>
                                            <input type="text" class="form-control form-control-lg" id="title"
                                                name="title" value="<%= idea.title %>" required maxlength="200">
                                        </div>

                                        <div class="mb-4">
                                            <label for="raw_idea" class="form-label fw-semibold">
                                                Idea Description <span class="text-danger">*</span>
                                            </label>
                                            <textarea class="form-control" id="raw_idea" name="raw_idea" rows="8"
                                                required maxlength="1000"><%= idea.raw_idea %></textarea>
                                            <div class="form-text">
                                                <span id="charCount">
                                                    <%= idea.raw_idea.length %>
                                                </span> / 1000 characters
                                            </div>
                                        </div>

                                        <% if (idea.status==='expanded' ) { %>
                                            <div class="alert alert-warning border-0 mb-4">
                                                <div class="d-flex">
                                                    <i class="bi bi-exclamation-triangle-fill me-3 mt-1"></i>
                                                    <div>
                                                        <strong>Note:</strong> Editing will preserve existing AI
                                                        analysis unless you choose to re-expand.
                                                    </div>
                                                </div>
                                            </div>

                                            <div class="form-check mb-4 p-3 border rounded bg-light">
                                                <input class="form-check-input" type="checkbox" id="reexpand"
                                                    name="reexpand" value="true">
                                                <label class="form-check-label fw-semibold text-primary" for="reexpand">
                                                    <i class="bi bi-magic me-2"></i>Re-expand this idea with AI
                                                </label>
                                                <div class="form-text mt-1 ms-4">
                                                    Check this to regenerate the AI analysis based on your new
                                                    description. Previous analysis will be overwritten.
                                                </div>
                                            </div>
                                            <% } %>

                                                <div class="d-flex gap-3">
                                                    <button type="submit" class="btn btn-primary btn-lg px-5">
                                                        <i class="bi bi-save-fill me-2"></i>Save Changes
                                                    </button>
                                                    <a href="/ideas/<%= idea.id %>"
                                                        class="btn btn-outline-secondary btn-lg px-4">
                                                        Cancel
                                                    </a>
                                                </div>
                                    </form>
                                </div>
                            </div>
                    </div>
                </div>
            </div>
        </main>

        <%- include('../partials/footer') %>

            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
            <script src="/js/main.js"></script>
            <script>
                const textarea = document.getElementById('raw_idea');
                const charCount = document.getElementById('charCount');

                textarea.addEventListener('input', function () {
                    charCount.textContent = textarea.value.length;
                });
            </script>
</body>

</html>